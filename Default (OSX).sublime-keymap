[
    // To use the suggested Key Bindings for Markdown2Dir...
    // ...just change the '/*' to '//*' at the beginning of line 8, and that's it.
    // "Markdown2Dir" will either attempt to generate files corresponding to the code fences from the
    // focused file using the rules defined in this package's settings, or will generate the code
    // fences in the currently focused markdown file from each of the files inside the same parent
    // directory as the currently focus markdown file.
    /*
    {
       "keys": ["command+alt+m"],
       "command": "dir2_markdown",
       "context": [
            { "key": "setting.enable_key_bindings", "operator": "equal", "operand": true },
            { "key": "selector", "operator": "equal", "operand": "text.plain, text.markdown" },
            { "key": "is_widget", "operator": "equal", "operand": false },
            { "key": "file_name", "operator": "regex_match", "operand": ".*\\.(md|markdown|mdown|mkd|mkdn|txt)$", "match_all": true }
        ]
    },
    {
       "keys": ["alt+shift+m"],
       "command": "markdown2_dir",
       "context": [
            { "key": "setting.enable_key_bindings", "operator": "equal", "operand": true },
            { "key": "selector", "operator": "equal", "operand": "text.plain, text.markdown" },
            { "key": "is_widget", "operator": "equal", "operand": false },
            { "key": "file_name", "operator": "regex_match", "operand": ".*\\.(md|markdown|mdown|mkd|mkdn|txt)$", "match_all": true }
        ]
    }
//*/
]
